# Required in production. Generate a high-entropy value with at least 32 characters.
BETTER_AUTH_SECRET=

# Canonical application origin. Production must use HTTPS.
BETTER_AUTH_URL=http://localhost:3000

# Used only by `npm run auth:provision`; never pass the password as a CLI argument.
PROVISION_USER_EMAIL=
PROVISION_USER_PASSWORD=
